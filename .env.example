# ── Umami Analytics secrets ──────────────────────────────────────────
# Copy this file to .env and fill in real values.
# Generate secrets with:  openssl rand -hex 24
# (Use hex, NOT base64 — base64 contains +/= which break the DB URL)
#
# NEVER commit .env to git — it's in .gitignore.

# PostgreSQL password for the Umami database
POSTGRES_PASSWORD=CHANGE_ME

# Umami application secret (used for session signing)
APP_SECRET=CHANGE_ME

# ── App settings (optional) ─────────────────────────────────────────
# Comma-separated list of allowed CORS origins
# ALLOWED_ORIGINS=https://lonklar.dk,https://www.lonklar.dk,http://localhost:5173
